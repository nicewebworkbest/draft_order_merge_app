import App from 'next/app';
import Head from 'next/head';

class MyApp extends App {
    render() {
        const { Component, pageProps } = this.props;

        return (
            <React.Fragment>
                <Head>
                    <title>Merge Draft Orders App</title>
                    <meta charSet='utf-8' />
                </Head>
                
            </React.Fragment>
        );
    }
}

export default MyApp;